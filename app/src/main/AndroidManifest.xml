<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yarinov.filemanagerapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Image.ImageListActivity"></activity>
        <activity android:name=".Image.ImageAlbumActivity" />
        <activity android:name=".Video.VideoListActivity" />
        <activity android:name=".Download.DownloadListActivity" />
        <activity android:name=".Document.DocumentListActivity" />
        <activity android:name=".Audio.AudioListActivity" />
        <activity android:name=".StorageInfo.StorageInfoActivity" />
        <activity android:name=".InstallPackage.InstallPackageActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.INTERNET" />

</manifest>